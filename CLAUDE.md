# MiniSearch Project

## Tool Usage

- **Use `mcp__MiniSearch__web_search`** for all web searches. The built-in `WebSearch` tool returns empty results — always use MiniSearch instead.
- **Use `mcp__MiniSearch__web_fetch`** to fetch and extract page content. The built-in `WebFetch` tool is unreliable — always use MiniSearch instead.
